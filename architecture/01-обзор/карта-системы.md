# Карта системы

```mermaid
graph LR
    TG[Пользователь Telegram] -->|URL карточки| Bot[aiogram Bot]
    Bot -->|detect_platform| UF[url_fetcher.py]
    UF -->|fetch HTML| WB[WB / Ozon]
    UF -->|extract_text| Parser[extract_product_text]
    Parser -->|текст карточки| Prompt[prompts.py]
    Prompt -->|промт аудита| AI[OpenRouter / YandexGPT]
    AI -->|AuditReport JSON| Gen[generator.py]
    Gen -->|отчёт| Bot
    Bot -->|5 сообщений| TG
    
    Bot -->|FSM state| SQLite[(SQLite)]
    Bot -->|whitelist| SQLite
    Gen -->|audit_log| SQLite
```
